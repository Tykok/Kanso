import type { Action } from "./types";

/**
 * Slice C — 19 to 23, cycle, triage, saved views and workload. Empty here so the barrel can name it from
 * day one and the branch that fills it edits no file another branch opens.
 */
export const organiseActions: readonly Action[] = [];
