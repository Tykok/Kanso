import type { Action } from "./types";

/**
 * Slice F — 27 and 28, the public surfaces. Empty here so the barrel can name it from
 * day one and the branch that fills it edits no file another branch opens.
 */
export const publikActions: readonly Action[] = [];
