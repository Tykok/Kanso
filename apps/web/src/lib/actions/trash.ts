import type { Action } from "./types";

/**
 * Slice E — 26, the trash. Empty here so the barrel can name it from
 * day one and the branch that fills it edits no file another branch opens.
 */
export const trashActions: readonly Action[] = [];
