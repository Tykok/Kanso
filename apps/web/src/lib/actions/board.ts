import type { Action } from "./types";

/**
 * Slice A — 04, the kanban board. Empty here so the barrel can name it from
 * day one and the branch that fills it edits no file another branch opens.
 */
export const boardActions: readonly Action[] = [];
