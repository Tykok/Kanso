import type { Action } from "./types";

/**
 * Slice D — 14 and 15, the inbox and the three states. Empty here so the barrel can name it from
 * day one and the branch that fills it edits no file another branch opens.
 */
export const inboxActions: readonly Action[] = [];
