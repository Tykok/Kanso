import type { Action } from "./types";

/**
 * Slice B — 07 and 22, documents. Empty here so the barrel can name it from
 * day one and the branch that fills it edits no file another branch opens.
 */
export const docsActions: readonly Action[] = [];
